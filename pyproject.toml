[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "p2p_utils"
version = "0.1.0"
dependencies = [
    "pytest",
]

[tool.setuptools.packages.find]
where = ["src"]
